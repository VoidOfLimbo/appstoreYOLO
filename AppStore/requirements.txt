# AppStore Requirements - CUDA 13.0 Compatible
# Python 3.10+ required
# CUDA Toolkit 13.0 required for GPU acceleration

# Core dependencies
pyinstaller>=6.0.0
opencv-contrib-python>=4.12.0  # Includes extra modules (CPU version)
numpy>=2.0.0,<2.6  # Compatible with CUDA 13.0
pillow>=12.0.0
pyqt5>=5.15.0
matplotlib>=3.10.0

# CUDA 13.0 acceleration packages
cupy-cuda13x>=13.0.0  # GPU array operations (CUDA 13.x)
tensorrt>=10.0.0  # NVIDIA TensorRT for model optimization (CUDA 13 support)

# Additional CUDA runtime
nvidia-cuda-runtime-cu13  # CUDA runtime libraries

# AI/ML frameworks
ultralytics>=8.0.0  # YOLOv8 for detection, classification, tracking
boxmot>=10.0.0  # Multi-object tracking (StrongSORT, DeepOCSORT, etc.)

# Jupyter integration
jupyterlab>=4.0.0  # Jupyter Lab for interactive notebooks
notebook>=7.0.0  # Classic Jupyter Notebook
ipykernel>=6.0.0  # IPython kernel for Jupyter

# Utilities
requests>=2.31.0  # HTTP requests for model downloads
tqdm>=4.66.0  # Progress bars

# Note: For OpenCV with CUDA support, you need to build from source
# The opencv-contrib-python package does not include CUDA binaries
# However, you can use CuPy for GPU acceleration with OpenCV operations
